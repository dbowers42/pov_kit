<% if name == '' %>
camera {
    location <%= location %>
    look_at  <%= look_at %>
}
<% else %>
    <% if is_local %>
#local <%= name %> = camera {
    location <%= location %>
    look_at  <%= look_at %>
    <% else %>
#declare <%= name %> = camera {
    location <%= location %>
    look_at  <%= look_at %>
    <% end %>
<% end %>